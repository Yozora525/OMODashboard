    var ageData2 = [20, 25, 30, 35, 40, 45, 50];
    var satisfactionData2 = [];
    var labels2 = [];
    var backgroundColors2 = [];
    var pieChart2;

    function getRandomSatisfaction() {
      return Math.floor(Math.random() * 10) + 1;
    }

    function generateData(gender) {
      satisfactionData2 = [];
      labels2 = [];
      backgroundColors2 = [];

      for (var i = 0; i < ageData2.length; i++) {
        var age2 = ageData2[i];
        var satisfaction2 = getRandomSatisfaction();

        satisfactionData2.push(satisfaction2);
        labels2.push('年龄' + age2);
        backgroundColors2.push(getRandomColor());
      }
    }

    function createPieChart() {
      var ctx2 = document.getElementById('pieChart').getContext('2d');
      pieChart = new Chart(ctx2, {
        type: 'pie',
        data: {
          labels2: labels2,
          datasets: [{
            data2: satisfactionData,
            backgroundColor2: backgroundColors
          }]
        },
        options: {
          responsive: true,
          maintainAspectRatio: false
        }
      });
    }

    function getRandomColor() {
      var letters2 = '0123456789ABCDEF';
      var color2 = '#';
      for (var i = 0; i < 6; i++) {
        color2 += letters2[Math.floor(Math.random() * 16)];
      }
      return color2;
    }

    function changeData(gender) {
      generateData(gender);
      updatePieChart();
    }

    function updatePieChart() {
      pieChart.data.datasets[0].data = satisfactionData;
      pieChart.data.labels = labels;
      pieChart.data.datasets[0].backgroundColor = backgroundColors;
      pieChart.update();
    }

    generateData('male');
    createPieChart();